// Alarm for spawning Enemy 1 every wave

// Enemy 1 spawns every wave
/*if(spawn_cnt < spawn_amt_oEnemy1){
	instance_create_depth(x,y,-1,oEnemy1);
	// Enemy 2 spawns every wave after wave 1
	if(global.level >= 2){
		if(spawn_cnt < spawn_amt_oEnemy2)
			instance_create_depth(x,y,-1,oEnemy2);
	}
	// Enemy 3 spawns every third wave
	if(global.level % 5 = 0){
		if(spawn_cnt < spawn_amt_oEnemy3)
			instance_create_depth(x,y,-1,oEnemy3);
	}
	// Increment spawned enemies count
	spawn_cnt++;
	alarm[2] = spawn_rate;
}*/